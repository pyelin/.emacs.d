# -*- mode: snippet -*-
# name: export transformer
# key: export-transformer
# --
const {Schemas} = require('../../../libs/src/enums/exported_datasets'),
      {id,
       mapTo,
       referenceId} = require('../column_generators');

module.exports = {
  columns: {
    ${2}Id: id(
      'A unique identifier for the ${3}.'
    ),
    personId: referenceId(
      'person.id',
      'A foreign key identifier to the person from whom the ${3} was recorded.'),
    visitId: referenceId(
      'visit.id',
      'A foreign key identifier to the visit during which the ${3} was recorded.'),
    conceptId: referenceId(
      'concept.id',
      ${4:conceptDescription}
    )
  }
};