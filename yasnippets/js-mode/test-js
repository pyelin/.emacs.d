# -*- mode: snippet -*-
# name: test-js
# key: test-js
# --
'use strict';

const chaiAsPromised = require('chai-as-promised'),
      chai = require('chai'),
      helpers = require('$1:path-to-helpers');

chai.use(chaiAsPromised);
const {expect} = chai;

describe($2:title, () => {
  let $3:instance;

  beforeEach(() => {
    return helpers.clearDb();
  });

  afterEach(() => {
    return helpers.clearDb();
  });

  it($4:test-title, () => {
    const promise = () => $5;
    expect(tasks[0]).to.eventually.deep.equal($5, $6);
  });
});
